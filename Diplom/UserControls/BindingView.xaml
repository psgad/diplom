<UserControl x:Class="Diplom.BindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Diplom"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="600"
             Margin="10" 
             Padding="10">
    <Border CornerRadius="22" 
            BorderThickness="1"
            BorderBrush="#fff"
            Padding="10,0">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Клавиша" Style="{DynamicResource subtitle}"
                   TextAlignment="Center"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>
            <TextBlock d:Text="D" Style="{DynamicResource subtitle}"
                   TextAlignment="Center"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Grid.Column="1"
                   x:Name="key"/>
            <TextBlock Text="Наименование" Style="{DynamicResource subtitle}"
                   TextAlignment="Center"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Grid.Column="2"/>
            <TextBox d:Text="Старт"
                 x:Name="name"
                 TextAlignment="Center"
                 VerticalAlignment="Center"
                 HorizontalAlignment="Stretch"
                 Grid.Column="3"/>
        </Grid>
    </Border>
</UserControl>
